[project]
name = "astral-eval"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "Aaron Taylor", email = "git@ataylor.io" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
astral-eval = "astral_eval:main"

[build-system]
requires = ["uv_build>=0.10.7,<0.11.0"]
build-backend = "uv_build"
