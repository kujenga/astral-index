[project]
name = "astral-index"
version = "0.1.0"
description = "AI-generated space technology newsletter"
readme = "README.md"
authors = [
    { name = "Aaron Taylor", email = "git@ataylor.io" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
astral-index = "astral_index:main"

[build-system]
requires = ["uv_build>=0.10.7,<0.11.0"]
build-backend = "uv_build"
